#include <stdio.h>

int main(){
    int a,b;
    int sum = a+b;
    printf("Enter a");
    scanf ("%d",&a);
    printf("Enter b");
    scanf("%d",&b);
    printf("u r sum is %d",&sum);
    
    
    return 0;
}

/*
compiler :- a computer program that translate c code into machin code.

hello.c ---> c compiler ----> a.out(linux) a.exe(window)

*/